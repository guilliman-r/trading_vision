# Copy this file to config.toml to override these defaults.

[app]
host = "127.0.0.1"
port = 8050
debug = false
default_symbol = "THYAO.IS"
default_interval = "1d"
chart_candle_limit = 500

[storage]
database_path = "var/trading_vision.sqlite3"

